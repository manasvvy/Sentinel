def call_llm(prompt: str) -> str:
    """
    Sends user input to the language model
    and returns the raw text response.
    """

    # TEMPORARY FAKE RESPONSE (stub)
    return "This is a placeholder response from the model."

